---
work_package_id: WP02
title: Conformance fixtures, manifest, dossier doc, fixture tests
dependencies:
- WP01
requirement_refs:
- FR-006
- FR-007
- FR-008
- FR-009
- FR-012
- NFR-001
- NFR-002
- NFR-004
planning_base_branch: main
merge_target_branch: main
branch_strategy: Planning artifacts for this mission were generated on the fix/events-32-force-required-wpstatuschanged feature branch. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into main per the operator brief in start-here.md Phase 1.
subtasks:
- T007
- T008
- T009
- T010
- T011
- T012
- T013
agent: claude
history:
- timestamp: '2026-05-18T06:45:00Z'
  actor: specify-orchestrator
  action: created
  notes: Generated by /spec-kitty.tasks
agent_profile: python-pedro
authoritative_surface: src/spec_kitty_events/conformance/fixtures/
execution_mode: code_change
mission_id: 01KRWWVJM6FSH9GW2GNC8VF1QW
mission_slug: force-required-review-rejection-01KRWWVJ
model: claude-sonnet-4-6
owned_files:
- src/spec_kitty_events/conformance/fixtures/edge_cases/invalid/wp_status_changed_unforced_in_review_to_planned.json
- src/spec_kitty_events/conformance/fixtures/edge_cases/invalid/wp_status_changed_unforced_in_progress_to_planned.json
- src/spec_kitty_events/conformance/fixtures/edge_cases/invalid/wp_status_changed_unforced_for_review_to_planned.json
- src/spec_kitty_events/conformance/fixtures/edge_cases/invalid/wp_status_changed_unforced_approved_to_planned.json
- src/spec_kitty_events/conformance/fixtures/edge_cases/valid/wp_status_changed_approved_rewind.json
- src/spec_kitty_events/conformance/fixtures/edge_cases/valid/wp_status_changed_forced_in_review_to_planned.json
- src/spec_kitty_events/conformance/fixtures/edge_cases/valid/wp_status_changed_forced_for_review_to_planned.json
- src/spec_kitty_events/conformance/fixtures/edge_cases/valid/wp_status_changed_forced_in_progress_to_planned.json
- src/spec_kitty_events/conformance/fixtures/manifest.json
- docs/consumer-contract-dossier-v2.4.0.md
- tests/unit/test_fixtures.py
role: implementer
tags: []
---

## ⚡ Do This First: Load Agent Profile

Before reading anything else, load your assigned agent profile:

```
/ad-hoc-profile-load python-pedro
```

You are operating as Python Pedro (role: implementer). The work here is
mostly JSON fixture editing, manifest maintenance, one Markdown doc
alignment, and a few new pytest cases. Adopt the profile's identity,
governance scope, and boundaries before reading the rest of this prompt.

## Objective

Extend the published conformance surface so every review-rejection family
pair has:

1. An **INVALID** fixture with `force=False` and BOTH `review_ref` and
   `reason` populated — proving the failure is isolated to "missing
   `force=True`".
2. A **VALID** fixture with `force=True` and a non-empty `reason` —
   proving the corrected runtime accepts the forced rollback.

Register every new/changed file in
`src/spec_kitty_events/conformance/fixtures/manifest.json`. Align
`docs/consumer-contract-dossier-v2.4.0.md` with the explicit-family-guard
mechanism. Extend `TestEdgeCaseFixtures` to load and validate the new
fixtures.

## Context

The full mission spec is at
`kitty-specs/force-required-review-rejection-01KRWWVJ/spec.md`. The plan
explains the design at
`kitty-specs/force-required-review-rejection-01KRWWVJ/plan.md`. The
behavior contract you are pinning at the fixture layer is at
`kitty-specs/force-required-review-rejection-01KRWWVJ/contracts/validate_transition.md`.

This WP DEPENDS on WP01 having landed. Confirm before starting:

```bash
cd /Users/robert/spec-kitty-dev/spec-kitty-20260518-082805-q0Lu7J/spec-kitty-events
uv run python -c "from spec_kitty_events.status import _REVIEW_REJECTION_FAMILY; print(len(_REVIEW_REJECTION_FAMILY))"
# expected: 4
```

If that command errors, WP01 has not landed and you should stop.

## Branch Strategy

- Planning base branch: `main`
- Final merge target: `main`
- Working branch: `fix/events-32-force-required-wpstatuschanged` (or the
  lane computed by `finalize-tasks` if running under a per-lane workspace).
- Execution worktree: allocated per `lanes.json`. Do not invent a branch.

## Per-Subtask Guidance

### T007 — Update existing INVALID fixture [P]

**Purpose**: Today's
`wp_status_changed_unforced_in_review_to_planned.json` has `reason=null`
and `review_ref=null`, so the existing `review_ref` guard fires and we
cannot prove the new family-guard is doing anything. Populate the optional
fields so "missing `force=True`" becomes the isolated cause.

**Steps**:

1. Edit
   `src/spec_kitty_events/conformance/fixtures/edge_cases/invalid/wp_status_changed_unforced_in_review_to_planned.json`:
   ```json
   {
     "wp_id": "WP09",
     "from_lane": "in_review",
     "to_lane": "planned",
     "actor": "user",
     "force": false,
     "reason": "review rejected: needs rework on edge cases",
     "execution_mode": "worktree",
     "review_ref": "feedback://mission-backward-transition-demo/WP09/2026-05-18-review.md",
     "evidence": null,
     "mission_slug": "mission-backward-transition-demo"
   }
   ```
2. Do not touch sibling fixtures here.

**Files**: as above.

**Validation**: JSON parses; manifest must be updated in T010 to match new
`expected_violations`.

### T008 — Create three new INVALID fixtures [P]

**Purpose**: FR-007 requires one INVALID-missing-force fixture per family
pair. Three pairs (`in_progress`, `for_review`, `approved`) still need one.

**Steps**: Create three new files, each modeled on T007's shape with the
appropriate `from_lane` and distinct `wp_id`:

1. `src/spec_kitty_events/conformance/fixtures/edge_cases/invalid/wp_status_changed_unforced_in_progress_to_planned.json`:
   ```json
   {
     "wp_id": "WP10",
     "from_lane": "in_progress",
     "to_lane": "planned",
     "actor": "user",
     "force": false,
     "reason": "abandoned: pivoting approach",
     "execution_mode": "worktree",
     "review_ref": "feedback://mission-backward-transition-demo/WP10/2026-05-18-rewind.md",
     "evidence": null,
     "mission_slug": "mission-backward-transition-demo"
   }
   ```
2. `src/spec_kitty_events/conformance/fixtures/edge_cases/invalid/wp_status_changed_unforced_for_review_to_planned.json`:
   ```json
   {
     "wp_id": "WP11",
     "from_lane": "for_review",
     "to_lane": "planned",
     "actor": "user",
     "force": false,
     "reason": "review intake rejected: scope not ready",
     "execution_mode": "worktree",
     "review_ref": "feedback://mission-backward-transition-demo/WP11/2026-05-18-intake.md",
     "evidence": null,
     "mission_slug": "mission-backward-transition-demo"
   }
   ```
3. `src/spec_kitty_events/conformance/fixtures/edge_cases/invalid/wp_status_changed_unforced_approved_to_planned.json`:
   ```json
   {
     "wp_id": "WP12",
     "from_lane": "approved",
     "to_lane": "planned",
     "actor": "user",
     "force": false,
     "reason": "approval revoked: regression discovered post-approval",
     "execution_mode": "worktree",
     "review_ref": "feedback://mission-backward-transition-demo/WP12/2026-05-18-revoke.md",
     "evidence": null,
     "mission_slug": "mission-backward-transition-demo"
   }
   ```

**Files**: three new JSON files.

**Validation**: Each parses as JSON.

### T009 — Create VALID fixtures for uncovered pairs [P]

**Purpose**: FR-008 requires one VALID-with-force fixture per family pair.
The existing `wp_status_changed_approved_rewind.json` covers `approved`.
The other three pairs need one.

**Steps**: Inspect
`src/spec_kitty_events/conformance/fixtures/edge_cases/valid/wp_status_changed_approved_rewind.json`
for the canonical shape, then mirror it:

1. `wp_status_changed_forced_in_progress_to_planned.json`:
   ```json
   {
     "wp_id": "WP20",
     "from_lane": "in_progress",
     "to_lane": "planned",
     "actor": "user",
     "force": true,
     "reason": "backward rewind: in_progress -> planned: scope changed",
     "execution_mode": "worktree",
     "review_ref": "feedback://mission-backward-transition-demo/WP20/scope-change.md",
     "evidence": null,
     "mission_slug": "mission-backward-transition-demo"
   }
   ```
2. `wp_status_changed_forced_for_review_to_planned.json`:
   ```json
   {
     "wp_id": "WP21",
     "from_lane": "for_review",
     "to_lane": "planned",
     "actor": "user",
     "force": true,
     "reason": "backward rewind: for_review -> planned: intake rejected",
     "execution_mode": "worktree",
     "review_ref": "feedback://mission-backward-transition-demo/WP21/intake-rejected.md",
     "evidence": null,
     "mission_slug": "mission-backward-transition-demo"
   }
   ```
3. `wp_status_changed_forced_in_review_to_planned.json`:
   ```json
   {
     "wp_id": "WP22",
     "from_lane": "in_review",
     "to_lane": "planned",
     "actor": "user",
     "force": true,
     "reason": "backward rewind: in_review -> planned: review rejected",
     "execution_mode": "worktree",
     "review_ref": "feedback://mission-backward-transition-demo/WP22/2026-05-18-review.md",
     "evidence": null,
     "mission_slug": "mission-backward-transition-demo"
   }
   ```

**Files**: three new JSON files under
`src/spec_kitty_events/conformance/fixtures/edge_cases/valid/`.

**Validation**: Each parses as JSON; mirrors `approved_rewind` shape.

### T010 — Update conformance manifest

**Purpose**: FR-009. Every new file must be discoverable and have correct
expectation metadata. The updated existing INVALID fixture must have its
`expected_violations` regenerated to include the substring `force=True`.

**Steps**:

1. Open `src/spec_kitty_events/conformance/fixtures/manifest.json`.
2. Find the existing entry for
   `wp_status_changed_unforced_in_review_to_planned.json`. Update its
   `expected_violations` (or equivalent expectation field) to include the
   substring `force=True` and the substring `review-rejection` so the new
   guard's output is the asserted failure mode. If the existing entry uses
   a different expectation shape (e.g. `expected_error_substrings`), use
   the same key.
3. Add manifest entries for each of the six new fixture files (3 INVALID + 3
   VALID), using sibling entries as templates. For each INVALID entry,
   `expected_violations` MUST contain a substring containing both `force=True`
   and the matching `from_lane` value (e.g. `in_progress`).
4. Sort entries to maintain whatever ordering the file already uses
   (alphabetical by path or insertion-ordered — match the existing
   convention).

**Files**: `src/spec_kitty_events/conformance/fixtures/manifest.json`.

**Validation**:

```bash
cd /Users/robert/spec-kitty-dev/spec-kitty-20260518-082805-q0Lu7J/spec-kitty-events
uv run python -c "import json; json.load(open('src/spec_kitty_events/conformance/fixtures/manifest.json'))"
```

And any existing manifest-consistency test must pass:

```bash
uv run pytest tests/unit/test_fixtures.py -q
```

### T011 — Align dossier doc [P]

**Purpose**: FR-006. The dossier currently describes the rejection
mechanism in terms of the matrix check. After WP01 the mechanism is the
explicit family-guard.

**Steps**:

1. Open `docs/consumer-contract-dossier-v2.4.0.md`.
2. Find the section starting near line 174 ("Review-Rejection Family")
   and the section near line 221 ("Unforced backward invalid").
3. Update the language so:
   - The four family pairs are enumerated in a table.
   - The enforcement mechanism is named as the "explicit review-rejection
     family guard" in `validate_transition()` (NOT "via the lane matrix
     check").
   - The canonical violation substrings are quoted exactly: ``force=True``
     and ``review-rejection``.
   - Bootstrap-planned (`from_lane=None`, `to_lane=planned`, `force=True`)
     is called out as NOT part of the family.
4. Do not edit any other section of the dossier.

**Files**: `docs/consumer-contract-dossier-v2.4.0.md`.

**Validation**: Markdown still renders cleanly; no broken links.

### T012 — Extend `TestEdgeCaseFixtures`

**Purpose**: FR-007/FR-008/FR-012. The new fixtures must be loaded by the
test suite and asserted against the new runtime behavior.

**Steps**:

1. Open `tests/unit/test_fixtures.py`. Locate `TestEdgeCaseFixtures`
   (currently around line 542 / 611).
2. If the class already parametrizes over a discovery glob, run
   `pytest tests/unit/test_fixtures.py::TestEdgeCaseFixtures -q` first to
   see whether the new fixtures are auto-discovered. If yes, only
   metadata-level assertions need updating (e.g., that the INVALID set has
   grown by 4).
3. If the class loads files by name, add explicit test methods or
   parametrize entries for each new fixture:
   - For each INVALID fixture: load the JSON, construct a
     `StatusTransitionPayload`, call `validate_transition`, assert
     `result.valid is False`, assert at least one violation contains both
     `force=True` and `review-rejection`.
   - For each VALID fixture: load and construct as above, call
     `validate_transition`, assert `result.valid is True`.
4. Do not remove or weaken any existing assertions.

**Files**: `tests/unit/test_fixtures.py`.

**Validation**: `pytest tests/unit/test_fixtures.py -q` exits 0.

### T013 — Run targeted + broad tests + reproduction snippet

**Purpose**: Pin the whole boundary green. NFR-001, NFR-002, NFR-004, SC-1.

**Steps**:

```bash
cd /Users/robert/spec-kitty-dev/spec-kitty-20260518-082805-q0Lu7J/spec-kitty-events

# Targeted (the start-here.md acceptance commands)
uv run pytest tests/unit/test_status.py::TestReviewRejectionFamily \
              tests/unit/test_fixtures.py::TestEdgeCaseFixtures -q

# Broad
uv run pytest tests/unit/test_status.py tests/unit/test_fixtures.py -q

# Strict type check (sanity, even though WP02 doesn't touch src code)
uv run mypy --strict src/spec_kitty_events

# Reproduction snippet (must now print False + violation with force=True)
uv run python - <<'PY'
from spec_kitty_events.status import (
    StatusTransitionPayload, Lane, ExecutionMode, validate_transition
)
payload = StatusTransitionPayload(
    mission_slug="m",
    wp_id="WP01",
    from_lane=Lane.IN_REVIEW,
    to_lane=Lane.PLANNED,
    actor="user",
    execution_mode=ExecutionMode.WORKTREE,
    force=False,
    reason="review rejected",
    review_ref="feedback://m/WP01/review.md",
)
print(validate_transition(payload))
PY
```

Paste the last 5 lines of each command's output into the commit message
for evidence.

**Files**: none (verification only).

**Validation**: All commands exit 0; reproduction prints `valid=False` and a
violation tuple containing `force=True` and `review-rejection`.

## Definition of Done

- All seven INVALID + VALID fixtures (3 new INVALID + 3 new VALID + 1
  updated INVALID, plus the pre-existing `approved_rewind` VALID) cover all
  four family pairs in both directions of the assertion.
- `manifest.json` registers every fixture with correct expectation metadata
  and parses as JSON.
- `docs/consumer-contract-dossier-v2.4.0.md` describes the explicit
  family-guard mechanism and the canonical violation substrings.
- `TestEdgeCaseFixtures` loads each new fixture and asserts the right
  validity + violation substrings.
- Both targeted and broad pytest commands exit 0.
- Reproduction snippet output captured.
- No edits to files outside `owned_files`.

## Reviewer Guidance

- Spot-check each INVALID fixture: it MUST have BOTH `review_ref` AND
  `reason` populated and `force=false`.
- Spot-check each VALID fixture: `force=true` and non-empty `reason`.
- Confirm the manifest's `expected_violations` for the new/updated INVALID
  entries names the substring `force=True`.
- Confirm the dossier no longer says "via the lane matrix check".
- Confirm reproduction snippet output is in the commit message.

## Implementation Command (after finalize-tasks)

```bash
spec-kitty agent action implement WP02 --agent <name>
```

## Activity Log

- 2026-05-18T07:30:17Z – claude – Done override: Status sync after mission merge (commit 78d01d4); WP02 actually completed and merged on 2026-05-18.
