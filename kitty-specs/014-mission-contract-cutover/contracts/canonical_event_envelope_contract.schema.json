{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "CanonicalEventEnvelopeContract",
  "description": "Planning contract for the canonical event envelope after the mission/build identity cutover.",
  "type": "object",
  "required": [
    "event_id",
    "event_type",
    "aggregate_id",
    "payload",
    "timestamp",
    "build_id",
    "node_id",
    "lamport_clock",
    "project_uuid",
    "correlation_id",
    "schema_version"
  ],
  "properties": {
    "build_id": {
      "type": "string",
      "minLength": 1,
      "description": "Canonical checkout or worktree identity."
    },
    "node_id": {
      "type": "string",
      "minLength": 1,
      "description": "Required causal emitter identity used for Lamport ordering and deterministic tie-breaking."
    },
    "schema_version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Canonical envelope version field. If this is the selected cutover signal, the cutover artifact must bind to this exact field by name and location."
    }
  },
  "additionalProperties": true
}
