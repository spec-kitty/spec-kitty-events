{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "DefaultChannelRef",
  "description": "Teamspace-configured default Slack channel for Decision Moment widening.",
  "type": "object",
  "additionalProperties": false,
  "required": ["channel_id"],
  "properties": {
    "channel_id": { "type": "string", "minLength": 1 },
    "name": { "type": "string" }
  }
}
