---
work_package_id: WP07
title: Release docs, version bump, and re-exports
dependencies:
- WP01
- WP02
- WP03
- WP04
- WP05
- WP06
requirement_refs:
- FR-015
- FR-018
- FR-019
planning_base_branch: main
merge_target_branch: main
branch_strategy: Planning artifacts for this feature were generated on main. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into main unless the human explicitly redirects the landing branch.
base_branch: kitty/mission-decision-moment-v1-contract-freeze-01KPWA0N
base_commit: 123bd1154ce20ed1456cfa1c027b29543783d55b
created_at: '2026-04-23T06:10:59.810634+00:00'
subtasks:
- T025
- T026
- T027
- T028
shell_pid: "36836"
agent: "claude:sonnet-4-7:python-reviewer:reviewer"
history:
- date: '2026-04-23'
  note: Initial prompt generated by /spec-kitty.tasks.
authoritative_surface: src/spec_kitty_events/
execution_mode: code_change
mission_id: 01KPWA0N4DX047S3NX734YRFVW
mission_slug: decision-moment-v1-contract-freeze-01KPWA0N
owned_files:
- pyproject.toml
- src/spec_kitty_events/__init__.py
- CHANGELOG.md
- COMPATIBILITY.md
tags: []
---

# WP07 — Release docs, version bump, and re-exports

## Objective

Close the 4.0.0 release loop:

1. Bump `pyproject.toml` version from `3.3.0` to `4.0.0`.
2. Re-export every new V1 symbol from `src/spec_kitty_events/__init__.py`.
3. Document the contract boundary in `CHANGELOG.md`.
4. Document compatibility posture in `COMPATIBILITY.md`.

This is the last WP to land. It depends on every other WP.

## Context

The charter mandates "deliberate compatibility review" for any envelope/payload/versioning change. This WP writes the permanent record.

See:
- `research.md` R-4, R-10 — version and docs policy.
- `data-model.md` §9 — migration notes.

## Branch strategy

- Planning/base branch: `main`
- Final merge target: `main`
- Execution worktree: assigned per lane; runs last.

## Subtasks

### T025 — Bump package version

**Purpose.** Publish 4.0.0 as a first-class breaking release.

**Steps.**

1. Edit `pyproject.toml`: change `version = "3.3.0"` to `version = "4.0.0"`.
2. Do not touch any other field.

**Files.** `pyproject.toml` (one line changed).

**Validation.**
- [ ] `python -c "import importlib.metadata; print(importlib.metadata.version('spec-kitty-events'))"` prints `4.0.0` after reinstall.

---

### T026 — Extend `__init__.py` to re-export V1 types

**Purpose.** Make every new public symbol importable directly from `spec_kitty_events`.

**Steps.**

1. Open `src/spec_kitty_events/__init__.py`.
2. Add imports (in existing style — sorted alphabetically within each import block):
   ```python
   from spec_kitty_events.decision_moment import (
       ClosureMessageRef,
       DefaultChannelRef,
       DiscussingSnapshotKind,
       OriginFlow,
       OriginSurface,
       SummaryBlock,
       SummarySource,
       TeamspaceRef,
       TerminalOutcome,
       ThreadRef,
       WideningChannel,
       WideningProjection,
   )
   from spec_kitty_events.collaboration import (
       ParticipantExternalRefs,
   )
   from spec_kitty_events.decisionpoint import (
       DECISION_POINT_WIDENED,
       DecisionPointDiscussingAdrPayload,
       DecisionPointDiscussingInterviewPayload,
       DecisionPointOpenedAdrPayload,
       DecisionPointOpenedInterviewPayload,
       DecisionPointResolvedAdrPayload,
       DecisionPointResolvedInterviewPayload,
       DecisionPointWidenedPayload,
   )
   ```
3. Add every new symbol to the `__all__` list. Preserve alphabetical order.
4. Do NOT remove any existing symbol.
5. Verify no naming collisions with existing symbols (e.g., `ThreadRef` is a new name and should be unique).

**Files.** `src/spec_kitty_events/__init__.py` (extend ~40 lines)

**Validation.**
- [ ] `python -c "from spec_kitty_events import OriginSurface, DecisionPointWidenedPayload, SummaryBlock, ParticipantExternalRefs, WideningProjection; print('OK')"` succeeds.
- [ ] `import spec_kitty_events` has no circular-import errors.
- [ ] `mypy --strict src/spec_kitty_events/__init__.py` clean.
- [ ] Existing imports from `spec_kitty_events import <3.x symbol>` still work.

---

### T027 — Update `CHANGELOG.md` with 4.0.0 section

**Purpose.** Permanent changelog of the contract boundary.

**Steps.**

1. Open `CHANGELOG.md`. Read the voice and format used by existing entries.
2. Prepend a new section at the top (below the file title / header), following the existing convention:

   ```markdown
   ## 4.0.0 — 2026-04-23

   ### Breaking

   - **DecisionPoint contract frozen for Decision Moment V1.** `DecisionPointOpenedPayload`, `DecisionPointDiscussingPayload`, and `DecisionPointResolvedPayload` are now Pydantic v2 discriminated unions keyed by `origin_surface` (`"adr"` or `"planning_interview"`). Existing 3.x ADR producers must add `origin_surface: "adr"` to every DecisionPoint payload.
   - **`DecisionPointResolved` (interview variant) requires `terminal_outcome`** (`"resolved" | "deferred" | "canceled"`). No grace period — 4.x validators fail closed. Cross-field constraints on `final_answer`/`rationale`/`other_answer` are enforced by a Pydantic `model_validator`.

   ### Added

   - `DecisionPointWidened` event type for Slack-backed widening of an interview-origin Decision Moment. Carries `channel`, `teamspace_ref`, `default_channel_ref`, `thread_ref`, `invited_participants`, `widened_by`, timestamps.
   - `WIDENED` state in `DecisionPointState` enum. Duplicate `DecisionPointWidened` for the same `decision_point_id` is idempotent.
   - Interview-origin fields on `DecisionPointOpened`: `origin_flow` (`charter`/`specify`/`plan`), `question`, `options`, `input_key`, `step_id`.
   - V1 projection fields on `ReducedDecisionPointState`: `origin_surface`, `origin_flow`, `question`, `options`, `input_key`, `step_id`, `widening`, `terminal_outcome`, `final_answer`, `other_answer`, `summary`, `actual_participants`, `resolved_by`, `closed_locally_while_widened`, `closure_message`.
   - Shared models: `SummaryBlock`, `TeamspaceRef`, `DefaultChannelRef`, `ThreadRef`, `ClosureMessageRef`, `WideningProjection`, `ParticipantExternalRefs`.
   - `ParticipantIdentity` extended with optional `external_refs` (Slack/Teamspace IDs carried losslessly for replay).
   - New reducer anomaly kind: `origin_mismatch` (events for the same `decision_point_id` with inconsistent `origin_surface`).
   - Six golden replay fixtures for every V1 scenario, plus invalid conformance fixtures for schema enforcement.

   ### Unchanged / compatible

   - `DecisionInputRequested` and `DecisionInputAnswered` payloads remain 3.x-compatible.
   - ADR semantics on `DecisionPointOpenedAdrPayload`, `DecisionPointDiscussingAdrPayload`, `DecisionPointResolvedAdrPayload` are preserved exactly.
   - `DecisionPointOverridden` accepts existing 3.x payloads; the optional new `origin_surface` field is additive.

   ### Behaviour rules

   - `DecisionInputAnswered` is emitted ONLY when a real final answer is written back. Deferred and canceled terminal outcomes do NOT emit a `DecisionInputAnswered`.
   - When the mission owner answers locally while a widened Slack discussion is open, `DecisionPointResolved.closed_locally_while_widened=true` is set. `closed_locally_while_widened=true` is only legal when a prior `DecisionPointWidened` exists for the same `decision_point_id`; otherwise the reducer raises an `invalid_transition` anomaly and the field is projected as `false`.

   ### Migration

   - 3.x ADR producers: add `origin_surface: "adr"` to every DecisionPoint payload. No other field changes required.
   - 3.x `DecisionInput*` producers: no changes required.
   - New interview-origin producers (`spec-kitty#757`, `spec-kitty#758`): use `DecisionPointOpenedInterviewPayload`, `DecisionPointWidenedPayload`, `DecisionPointDiscussingInterviewPayload`, `DecisionPointResolvedInterviewPayload`.

   ### Internal

   - `DECISIONPOINT_SCHEMA_VERSION` bumped from `"2.6.0"` to `"3.0.0"`.
   ```

3. Keep wording consistent with prior entries (tense, bullet style).

**Files.** `CHANGELOG.md` (prepend new section, existing entries unchanged).

**Validation.**
- [ ] Markdown lints (if lint is configured) pass.
- [ ] `grep -n "## 4.0.0" CHANGELOG.md` shows exactly one match.
- [ ] Prior 3.x entries are unchanged.

---

### T028 — Update `COMPATIBILITY.md` with Decision Moment V1 (4.0.0) block

**Purpose.** Formal compatibility posture document for downstream consumers.

**Steps.**

1. Open `COMPATIBILITY.md`. Read the existing structure (section headings, tables).
2. Add a new section `## Decision Moment V1 (4.0.0)` (placement follows the repo's existing section order — typically appended as the newest contract boundary):

   ```markdown
   ## Decision Moment V1 (4.0.0)

   ### Scope

   - **Breaking for DecisionPoint.** The `DecisionPoint*` event family (excluding `DecisionPointOverridden`) now carries `origin_surface` and supports discriminated-union payloads. `DecisionPointResolved` (interview variant) requires `terminal_outcome`.
   - **Compatible for DecisionInput.** `DecisionInputRequested` and `DecisionInputAnswered` payloads are unchanged. 3.x consumers continue to validate.

   ### Producer migration

   | Producer                | 3.x action                         | 4.0.0 action                                                   |
   |-------------------------|------------------------------------|----------------------------------------------------------------|
   | ADR DecisionPoint       | Emit 3.x payload                   | Add `origin_surface: "adr"` to every payload                    |
   | Interview DecisionPoint | (n/a — didn't exist)               | Use `origin_surface: "planning_interview"` + V1 fields          |
   | DecisionInput* events   | Emit as-is                         | No change                                                       |

   ### Consumer migration

   | Consumer                              | 3.x action                                       | 4.0.0 action                                                                 |
   |---------------------------------------|--------------------------------------------------|------------------------------------------------------------------------------|
   | DecisionPoint replay / reducer        | Reduce 3.x ADR payloads                          | Reduce ADR + V1 interview events via the single reducer (discriminated by `origin_surface`) |
   | DecisionInput* consumers              | Consume as-is                                    | No change                                                                    |
   | Slack orchestrator                    | (n/a)                                            | Subscribe to `DecisionPointWidened`; post closure message on `DecisionPointResolved` |
   | Teamspace projection                  | (n/a)                                            | Project V1 fields from `DecisionPointResolved` interview variant             |

   ### Terminal outcome / write-back rules

   - `DecisionInputAnswered` is emitted ONLY when `DecisionPointResolved.terminal_outcome == "resolved"` AND `final_answer` is populated. Deferred and canceled outcomes do NOT emit a `DecisionInputAnswered` (no answer exists).
   - `DecisionPointResolved.closed_locally_while_widened=true` is legal only when a prior `DecisionPointWidened` exists for the same `decision_point_id`. Reducers raise an anomaly (`kind="invalid_transition"`) and project the field as `false` if the precondition is not met.

   ### No grace period

   4.x validators fail closed on missing `terminal_outcome` or missing `origin_surface`. There is no temporary permissive path. Downstream consumers must migrate deliberately against this contract boundary.
   ```

3. If `COMPATIBILITY.md` already has a "Decision Moment" section or a DecisionPoint section, extend it rather than duplicating.

**Files.** `COMPATIBILITY.md` (extend)

**Validation.**
- [ ] Markdown renders cleanly in a viewer.
- [ ] References to `DecisionPointWidened`, `terminal_outcome`, `closed_locally_while_widened` are consistent with the implemented contract.

## Definition of Done

- [ ] All four subtasks complete.
- [ ] `pyproject.toml` version is `4.0.0`.
- [ ] `from spec_kitty_events import OriginSurface, DecisionPointWidenedPayload, SummaryBlock, WideningProjection, ParticipantExternalRefs, TerminalOutcome, DECISION_POINT_WIDENED` succeeds.
- [ ] `CHANGELOG.md` has a `## 4.0.0` section with Breaking, Added, Unchanged, Behaviour rules, and Migration blocks.
- [ ] `COMPATIBILITY.md` has a Decision Moment V1 (4.0.0) block.
- [ ] `pytest -q` green (the full repo suite).
- [ ] `mypy --strict src/spec_kitty_events` clean.
- [ ] `git diff` touches only owned files.

## Risks

- **Re-export collisions.** Double-check `__all__` for duplicate names. If a 3.x symbol has the same name as a new V1 symbol, rename the V1 symbol — do NOT shadow.
- **Changelog voice drift.** Match the existing tense and bullet style. If prior entries use "Add" not "Added", match that.
- **Forgotten symbols.** Verify the list of re-exports matches every new public symbol introduced by WP01 and WP02.

## Reviewer guidance

- Open the regenerated `__init__.py` and confirm every new symbol is importable and appears in `__all__`.
- Open `CHANGELOG.md` and confirm the 4.0.0 section lists the breaking changes accurately.
- Open `COMPATIBILITY.md` and confirm the producer/consumer migration tables reflect the actual contract rules.

## Implementation command

```bash
spec-kitty agent action implement WP07 --agent <your-agent-name>
```

## Activity Log

- 2026-04-23T06:11:01Z – claude:sonnet-4-7:python-implementer:implementer – shell_pid=24530 – Assigned agent via action command
- 2026-04-23T06:19:39Z – claude:sonnet-4-7:python-implementer:implementer – shell_pid=24530 – WP07 done: 4.0.0 release sweep + WP04 regression fix
- 2026-04-23T06:20:16Z – claude:sonnet-4-7:python-reviewer:reviewer – shell_pid=33422 – Started review via action command
- 2026-04-23T06:25:06Z – claude:sonnet-4-7:python-reviewer:reviewer – shell_pid=33422 – Moved to planned
- 2026-04-23T06:25:39Z – claude:sonnet-4-7:python-implementer:implementer – shell_pid=35467 – Started implementation via action command
- 2026-04-23T06:28:28Z – claude:sonnet-4-7:python-implementer:implementer – shell_pid=35467 – WP07 cycle 1 fix: reducer fix re-applied after merge, manifest entries added
- 2026-04-23T06:28:49Z – claude:sonnet-4-7:python-reviewer:reviewer – shell_pid=36836 – Started review via action command
- 2026-04-23T06:31:37Z – claude:sonnet-4-7:python-reviewer:reviewer – shell_pid=36836 – Review passed cycle 2: 4.0.0 release sweep + merge-regression fixes verified green
- 2026-04-23T06:35:55Z – claude:sonnet-4-7:python-reviewer:reviewer – shell_pid=36836 – Moved to done
