---
work_package_id: WP01
title: Shared V1 foundation models
dependencies: []
requirement_refs:
- FR-009
- FR-012
- FR-013
planning_base_branch: main
merge_target_branch: main
branch_strategy: Planning artifacts for this feature were generated on main. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into main unless the human explicitly redirects the landing branch.
subtasks:
- T001
- T002
- T003
- T004
history:
- date: '2026-04-23'
  note: Initial prompt generated by /spec-kitty.tasks.
authoritative_surface: src/spec_kitty_events/
execution_mode: code_change
mission_id: 01KPWA0N4DX047S3NX734YRFVW
mission_slug: decision-moment-v1-contract-freeze-01KPWA0N
owned_files:
- src/spec_kitty_events/decision_moment.py
- src/spec_kitty_events/collaboration.py
- tests/unit/test_decision_moment_models.py
- tests/unit/test_collaboration_models.py
tags: []
---

# WP01 — Shared V1 foundation models

## Objective

Land the reusable foundation for the `spec-kitty-events 4.0.0` Decision Moment V1 contract:

1. Create a new module `src/spec_kitty_events/decision_moment.py` holding V1 enums and shared Pydantic models that every subsequent WP imports.
2. Extend `ParticipantIdentity` in `src/spec_kitty_events/collaboration.py` with an optional `external_refs` block, preserving full 3.x compatibility.
3. Lock both additions with unit tests under `tests/unit/`.

This WP has no dependencies. It is the foundation every other WP (especially WP02) compiles against.

## Context

The `spec-kitty-events 4.0.0` contract freeze introduces Decision Moment V1. Several cross-cutting shared models are used by both the new `DecisionPointWidened` event and the refactored `DecisionPointResolved` payload (interview variant):

- A canonical participant-identity extension with optional Slack/Teamspace references (FR-012, FR-013).
- A structured `SummaryBlock` for resolved decisions with provenance (FR-009).
- Typed references for Slack threads, teamspaces, channels, and closure messages (FR-005, FR-010, FR-011).
- Shared enums (`OriginSurface`, `OriginFlow`, `TerminalOutcome`, `SummarySource`, `WideningChannel`, `DiscussingSnapshotKind`).

See the mission's `data-model.md` §1 and §2.1 for exhaustive field definitions and `research.md` R-2 and R-5 for rationale.

## Branch strategy

- Planning/base branch: `main`
- Final merge target: `main`
- Execution worktree: assigned per lane by `spec-kitty agent mission finalize-tasks` (see `lanes.json`). Run `spec-kitty agent action implement WP01 --agent <name>` to enter the worktree.

## Subtasks

### T001 — Create `decision_moment.py` with V1 enums and shared models

**Purpose.** Provide a single authoritative location for V1 shared types that `decisionpoint.py` (WP02) and downstream packages can import.

**Steps.**

1. Create `src/spec_kitty_events/decision_moment.py` with the following top-level structure:
   - Module docstring: `"""Decision Moment V1 shared models and enums (spec-kitty-events 4.0.0)."""`
   - `from __future__ import annotations`
   - Imports: `datetime`, `Enum`, `Optional`, `Tuple`; `BaseModel, ConfigDict, Field` from pydantic.
2. Add enums (str-backed; match exact values from `data-model.md`):
   - `OriginSurface`: `ADR = "adr"`, `PLANNING_INTERVIEW = "planning_interview"`.
   - `OriginFlow`: `CHARTER = "charter"`, `SPECIFY = "specify"`, `PLAN = "plan"`.
   - `TerminalOutcome`: `RESOLVED = "resolved"`, `DEFERRED = "deferred"`, `CANCELED = "canceled"`.
   - `SummarySource`: `SLACK_EXTRACTION = "slack_extraction"`, `MANUAL = "manual"`, `MISSION_OWNER_OVERRIDE = "mission_owner_override"`.
   - `WideningChannel`: `SLACK = "slack"`.
   - `DiscussingSnapshotKind`: `PARTICIPANT_CONTRIBUTION = "participant_contribution"`, `DIGEST = "digest"`, `OWNER_NOTE = "owner_note"`.
3. Add shared Pydantic models (all with `model_config = ConfigDict(frozen=True, extra="forbid")` unless otherwise noted):
   - `SummaryBlock`: `text: str (min_length=1)`, `source: SummarySource`, `extracted_at: Optional[datetime] = None`, `candidate_answer: Optional[str] = None`.
   - `TeamspaceRef`: `teamspace_id: str (min_length=1)`, `name: Optional[str] = None`.
   - `DefaultChannelRef`: `channel_id: str (min_length=1)`, `name: Optional[str] = None`.
   - `ThreadRef`: `slack_team_id: Optional[str] = None`, `channel_id: str (min_length=1)`, `thread_ts: str (min_length=1)`, `url: Optional[str] = None`.
   - `ClosureMessageRef`: `channel_id: str (min_length=1)`, `thread_ts: str (min_length=1)`, `message_ts: str (min_length=1)`, `url: Optional[str] = None`.
   - `WideningProjection`: projection model used by `ReducedDecisionPointState` in WP02. Fields: `channel: WideningChannel`, `teamspace_ref: TeamspaceRef`, `default_channel_ref: DefaultChannelRef`, `thread_ref: ThreadRef`, `invited_participants: Tuple["ParticipantIdentity", ...]` (use string forward reference and `from __future__ import annotations`; import `ParticipantIdentity` lazily in the module using `TYPE_CHECKING` to avoid circular imports — or place `WideningProjection` in `decisionpoint.py` instead if the circular import is problematic). `widened_by: str (min_length=1)`, `widened_at: datetime`.
4. Export `__all__` listing every public symbol.

**Files.**
- `src/spec_kitty_events/decision_moment.py` (new, ~160 lines)

**Validation.**
- [ ] `python -c "from spec_kitty_events.decision_moment import OriginSurface, SummaryBlock, ThreadRef; print('OK')"` succeeds.
- [ ] `mypy --strict src/spec_kitty_events/decision_moment.py` clean.
- [ ] All enums are `str`-backed (serialize as string values on the wire).

**Edge cases.**
- `SummaryBlock.text` must reject empty string.
- `ThreadRef.thread_ts` must reject empty string.
- Circular import risk: if `WideningProjection` needs `ParticipantIdentity`, use `TYPE_CHECKING` guard and stringified annotation, or relocate `WideningProjection` to `decisionpoint.py` in WP02. Prefer the latter if the import edge becomes awkward.

---

### T002 — Extend `collaboration.py` with `ParticipantExternalRefs` and `ParticipantIdentity.external_refs`

**Purpose.** Carry optional Slack/Teamspace identity alongside the canonical `participant_id` so replay and downstream rendering don't need live lookups (FR-012, FR-013).

**Steps.**

1. In `src/spec_kitty_events/collaboration.py`, near the existing `ParticipantIdentity` (around lines 54–70), add `ParticipantExternalRefs`:
   ```python
   class ParticipantExternalRefs(BaseModel):
       """Optional cross-channel identity references for a ParticipantIdentity."""

       model_config = ConfigDict(frozen=True, extra="forbid")

       slack_user_id: Optional[str] = Field(None, min_length=1)
       slack_team_id: Optional[str] = Field(None, min_length=1)
       teamspace_member_id: Optional[str] = Field(None, min_length=1)

       @model_validator(mode="after")
       def _at_least_one(self) -> "ParticipantExternalRefs":
           if not any((self.slack_user_id, self.slack_team_id, self.teamspace_member_id)):
               raise ValueError(
                   "ParticipantExternalRefs must populate at least one of "
                   "slack_user_id, slack_team_id, teamspace_member_id"
               )
           return self
   ```
2. Extend `ParticipantIdentity` with `external_refs: Optional[ParticipantExternalRefs] = None`. Keep all existing fields unchanged. Keep `model_config = ConfigDict(frozen=True)` (no `extra="forbid"` — it was already permissive).
3. Add `model_validator` import from pydantic at the top of the file if not already imported.
4. Confirm `from spec_kitty_events.collaboration import ParticipantExternalRefs` works.
5. Do not modify any existing test or consumer of `ParticipantIdentity`.

**Files.**
- `src/spec_kitty_events/collaboration.py` (extend ~15 lines)

**Validation.**
- [ ] `mypy --strict src/spec_kitty_events/collaboration.py` clean.
- [ ] Existing tests in `tests/unit/test_collaboration_models.py` still pass without changes.
- [ ] `ParticipantIdentity(participant_id="p1", participant_type="human")` still validates (no external_refs required).

**Edge cases.**
- A caller that provides `external_refs={}` must fail validation (empty object with no fields set triggers `_at_least_one`). This is the explicit "invalid `external_refs` shape" scenario called out in the spec (edge cases).
- Existing tests that construct `ParticipantIdentity` with only required fields must continue to pass.

---

### T003 — Unit tests for V1 shared models

**Purpose.** Lock the shape and validation rules of every new model in `decision_moment.py`.

**Steps.**

1. Create `tests/unit/test_decision_moment_models.py` (new file). Use the existing `tests/unit/test_collaboration_models.py` as a style template (pytest functions, descriptive names, no class wrappers unless already the convention).
2. Enum coverage:
   - `test_origin_surface_values` — assert `OriginSurface.ADR.value == "adr"`, `OriginSurface.PLANNING_INTERVIEW.value == "planning_interview"`.
   - `test_origin_flow_values` — assert all three values (`charter`, `specify`, `plan`).
   - `test_terminal_outcome_values` — assert `resolved`, `deferred`, `canceled`.
   - `test_summary_source_values` — assert all three values.
   - `test_widening_channel_slack_only` — assert only `slack` exists.
   - `test_discussing_snapshot_kind_values` — assert `participant_contribution`, `digest`, `owner_note`.
3. `SummaryBlock`:
   - `test_summary_block_minimal` — construct with only required `text` + `source`, assert `extracted_at is None`, `candidate_answer is None`.
   - `test_summary_block_full` — construct with all four fields.
   - `test_summary_block_rejects_empty_text` — `pytest.raises(ValidationError)`.
   - `test_summary_block_each_source_roundtrips` — parameterize over `SummarySource`, dump to json, reload, assert equal.
4. Ref models:
   - `test_teamspace_ref_minimal` / `test_teamspace_ref_rejects_empty_id`.
   - `test_default_channel_ref_minimal` / rejects empty id.
   - `test_thread_ref_minimal` (only `channel_id` + `thread_ts` required) / `test_thread_ref_rejects_empty_channel_id` / `test_thread_ref_rejects_empty_thread_ts`.
   - `test_closure_message_ref_minimal` / rejects empty id fields.
5. `WideningProjection`:
   - `test_widening_projection_constructs_with_empty_participants` — `invited_participants=()` is valid.
   - `test_widening_projection_serializes_channel_as_slack` — model_dump shows `"channel": "slack"`.

**Files.**
- `tests/unit/test_decision_moment_models.py` (new, ~170 lines)

**Validation.**
- [ ] `pytest tests/unit/test_decision_moment_models.py -q` green.
- [ ] Coverage of `decision_moment.py` ≥95% (confirm via `pytest --cov`).

**Edge cases.**
- Pydantic `ValidationError` import path (`from pydantic import ValidationError`).
- `extracted_at` must accept ISO datetime strings via `model_validate_json`.

---

### T004 — Unit tests for `ParticipantIdentity.external_refs`

**Purpose.** Lock the new optional field and the at-least-one-field validator.

**Steps.**

1. Extend `tests/unit/test_collaboration_models.py` with a new section (keep existing tests intact). If the file already uses a class, add to that class; otherwise append functions.
2. Tests to add:
   - `test_participant_identity_without_external_refs_still_valid` — construct with just `participant_id` + `participant_type`. Assert `external_refs is None`.
   - `test_participant_identity_with_slack_user_id_only` — construct `ParticipantExternalRefs(slack_user_id="U123")` and embed in `ParticipantIdentity`. Assert roundtrip via model_dump → model_validate preserves equality.
   - `test_participant_identity_with_all_external_refs` — all three fields populated.
   - `test_external_refs_rejects_all_none` — `ParticipantExternalRefs()` with no fields must raise `ValidationError` with a message mentioning the at-least-one requirement.
   - `test_external_refs_rejects_empty_strings` — `ParticipantExternalRefs(slack_user_id="")` must raise.
   - `test_external_refs_forbids_extra_fields` — passing `ParticipantExternalRefs(slack_user_id="U123", foo="bar")` must raise (extra=forbid).

**Files.**
- `tests/unit/test_collaboration_models.py` (extend ~80 lines)

**Validation.**
- [ ] `pytest tests/unit/test_collaboration_models.py -q` green.
- [ ] Existing tests in the file still pass.

**Edge cases.**
- `model_validate({"slack_user_id": "U123"})` on `ParticipantExternalRefs` must work (dict-form input).
- `json.dumps(external_refs.model_dump(mode="json"))` round-trips.

## Definition of Done

- [ ] All four subtasks complete.
- [ ] `pytest tests/unit/test_decision_moment_models.py tests/unit/test_collaboration_models.py -q` green.
- [ ] `mypy --strict src/spec_kitty_events/decision_moment.py src/spec_kitty_events/collaboration.py` clean.
- [ ] No existing test newly fails (`pytest -q` for the whole repo should pass or fail only due to unrelated pre-existing issues, if any).
- [ ] `git diff` shows changes only in the four owned files.
- [ ] No imports from downstream WPs' not-yet-existent symbols.

## Reviewer guidance

- Verify `ParticipantIdentity.external_refs` is truly optional: any existing call site that doesn't pass it must still construct successfully.
- Verify `ParticipantExternalRefs._at_least_one` rejects both `ParticipantExternalRefs()` and `ParticipantExternalRefs(slack_user_id="")`.
- Verify new module `decision_moment.py` has no runtime dependency on `decisionpoint.py` (keep the import graph one-way: decisionpoint.py imports decision_moment.py later, not vice versa).
- Verify all enums are `str`-backed (not plain `Enum`) so JSON serialization produces string values, matching the committed schemas.

## Implementation command

```bash
spec-kitty agent action implement WP01 --agent <your-agent-name>
```

## Activity Log

- 2026-04-23T06:35:45Z – unknown – Moved to done
