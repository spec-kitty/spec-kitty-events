# Work Packages: Per-User Identity in Connector Events

**Inputs**: Design documents from `kitty-specs/013-per-user-identity-connector-events/`
**Prerequisites**: plan.md (required), spec.md (user stories), research.md, data-model.md, contracts/

**Tests**: Included — this is a contract library where tests are the primary deliverable alongside models.

**Organization**: Fine-grained subtasks (`Txxx`) roll up into work packages (`WPxx`). Each work package must be independently deliverable and testable.

**Prompt Files**: Each work package references a matching prompt file in `tasks/` generated by `/spec-kitty.tasks`.

## Subtask Format: `[Txxx] [P?] Description`
- **[P]** indicates the subtask can proceed in parallel (different files/components).
- Include precise file paths or modules.

---

## Work Package WP01: Payload Models & Constants (Priority: P0)

**Goal**: Add `user_id` to existing payloads, define new event types and payload models, bump schema version.
**Independent Test**: All new/modified Pydantic models validate, serialize, and round-trip correctly. `mypy --strict` passes.
**Prompt**: `tasks/WP01-payload-models-and-constants.md`
**Requirement Refs**: FR-001, FR-002, FR-003, FR-004, FR-006, C-001, C-002, C-003, C-004
**Estimated Prompt Size**: ~350 lines

### Included Subtasks
- [x] T001 Bump `CONNECTOR_SCHEMA_VERSION` from `"2.7.0"` to `"2.8.0"` in `src/spec_kitty_events/connector.py`
- [x] T002 Add `user_id: Optional[str] = None` to all 5 existing connector payload models in `src/spec_kitty_events/connector.py`
- [x] T003 Add `USER_CONNECTED` and `USER_DISCONNECTED` event type constants, update `CONNECTOR_EVENT_TYPES` frozenset in `src/spec_kitty_events/connector.py`
- [x] T004 Create `UserConnectedPayload` and `UserDisconnectedPayload` models with required `user_id` in `src/spec_kitty_events/connector.py`
- [x] T005 Create `UserConnectionStatus` model in `src/spec_kitty_events/connector.py`

### Implementation Notes
- All models use `ConfigDict(frozen=True)` matching existing pattern.
- `user_id` on existing payloads: `Optional[str] = None` (backward compatible).
- `user_id` on new payloads: `str = Field(..., min_length=1)` (required).
- `UserConnectedPayload` shares common fields (connector_id, connector_type, provider, mission_id, project_uuid, actor_id, actor_type, endpoint_url, recorded_at) plus required `user_id`.
- `UserDisconnectedPayload` adds optional `reason: str = ""`.
- `UserConnectionStatus` has `user_id: str`, `state: ConnectorState`, `last_event_at: Optional[datetime] = None`.
- Update `_EVENT_TO_STATE` mapping: `USER_CONNECTED` → `ConnectorState.PROVISIONED`, `USER_DISCONNECTED` → `ConnectorState.REVOKED`.
- Update `_EVENT_TO_PAYLOAD` mapping with new payload types.
- Update `ConnectorPayload` union type to include new payloads.

### Parallel Opportunities
- T001, T003, T005 can be done simultaneously if working in different sections of `connector.py`.

### Dependencies
- None (starting package).

### Risks & Mitigations
- Pydantic v2 `AnyHttpUrl` serialization: existing `field_serializer` pattern already handles this.
- `from __future__ import annotations` + mypy: use distinct variable names per type branch (established pattern).

---

## Work Package WP02: Reducer & Roster Logic (Priority: P0)

**Goal**: Extend `ReducedConnectorState` with `user_connections` roster and update `reduce_connector_events()` to build per-user state from `user_id` fields.
**Independent Test**: Reducer produces correct roster for streams with/without user_id, including mixed binding-level and user-level events. Backward compatibility verified with pre-migration event streams.
**Prompt**: `tasks/WP02-reducer-and-roster.md`
**Requirement Refs**: FR-005, FR-007, NFR-001, C-002
**Estimated Prompt Size**: ~300 lines

### Included Subtasks
- [x] T006 Add `user_connections: Tuple[UserConnectionStatus, ...] = ()` to `ReducedConnectorState` in `src/spec_kitty_events/connector.py`
- [x] T007 Update `reduce_connector_events()` to build per-user roster from `user_id` on binding-level events in `src/spec_kitty_events/connector.py`
- [x] T008 Handle `UserConnected`/`UserDisconnected` in reducer: skip binding-level state transitions, update roster only in `src/spec_kitty_events/connector.py`
- [x] T009 Add anomaly handling for `UserDisconnected` without prior `UserConnected` in `src/spec_kitty_events/connector.py`

### Implementation Notes
- Mutable roster dict `Dict[str, Tuple[ConnectorState, Optional[datetime]]]` keyed by `user_id` during fold, frozen to `Tuple[UserConnectionStatus, ...]` at output.
- For binding-level events with `user_id` set: update roster with the event's target state and `recorded_at`.
- For `UserConnected`/`UserDisconnected`: update roster but skip `current_state`/`transition_log` (binding-level state unaffected).
- Events without `user_id` (legacy): no roster update, binding-level processing unchanged.
- Sort roster by `user_id` in output for determinism.
- `UserDisconnected` for unknown user: record anomaly (kind="invalid_transition").

### Parallel Opportunities
- T006 (model change) can proceed before T007-T009 (logic changes).

### Dependencies
- Depends on WP01 (new models, constants, and event type mappings must exist).

### Risks & Mitigations
- Determinism: roster must be sorted by `user_id` to ensure same output regardless of event ordering.
- Backward compat: verify with event stream containing zero `user_id` fields — roster must be empty, binding-level state identical to pre-change.

---

## Work Package WP03: Exports, Schemas & Tests (Priority: P1)

**Goal**: Wire new symbols into `__init__.py`, regenerate JSON schemas, update/extend all test suites.
**Independent Test**: `python3.11 -m pytest` passes with 98%+ coverage, `mypy --strict` passes, JSON schemas match Pydantic models.
**Prompt**: `tasks/WP03-exports-schemas-tests.md`
**Requirement Refs**: FR-001–FR-007, NFR-001, NFR-002, NFR-003, C-001–C-004
**Estimated Prompt Size**: ~400 lines

### Included Subtasks
- [x] T010 Update `__init__.py` with new exports: `USER_CONNECTED`, `USER_DISCONNECTED`, `UserConnectedPayload`, `UserDisconnectedPayload`, `UserConnectionStatus` in `src/spec_kitty_events/__init__.py`
- [x] T011 Regenerate JSON schemas for all modified/new connector payloads via `src/spec_kitty_events/schemas/generate.py`
- [x] T012 [P] Update unit tests for modified payload models (user_id field), new payloads, and constants in `tests/unit/test_connector.py`
- [x] T013 [P] Update reducer tests for roster functionality and backward compatibility in `tests/test_connector_reducer.py`
- [x] T014 [P] Extend property tests to include `UserConnected`/`UserDisconnected` in event pool in `tests/property/test_connector_determinism.py`

### Implementation Notes
- `__init__.py`: Add new block "Connector Per-User Identity (2.8.0)" with re-exports. Update `__all__` list. Update docstring version notes.
- Schema regeneration: run `python3.11 -m spec_kitty_events.schemas.generate` to produce/update JSON schema files. New schemas: `user_connected_payload.schema.json`, `user_disconnected_payload.schema.json`, `user_connection_status.schema.json`. Existing connector schemas regenerated with `user_id` field.
- Unit tests: verify `user_id=None` default on existing payloads, `user_id` required on new payloads, `CONNECTOR_SCHEMA_VERSION == "2.8.0"`, `CONNECTOR_EVENT_TYPES` has 7 members.
- Reducer tests: test roster with mixed events, pre-migration stream (empty roster), duplicate user events, UserDisconnected without UserConnected anomaly.
- Property tests: add `UserConnected`/`UserDisconnected` payloads to Hypothesis event pool, verify determinism with 200+ examples.

### Parallel Opportunities
- T012, T013, T014 are in different test files and can proceed in parallel.

### Dependencies
- Depends on WP02 (reducer must be complete before testing).

### Risks & Mitigations
- Schema drift: CI drift check will catch mismatches. Regenerate all connector schemas, not just new ones.
- Test coverage: aim for 98%+. Cover all edge cases from spec (no user_id, coinciding actor_id/user_id, UserDisconnected anomaly).

---

## Dependency & Execution Summary

- **Sequence**: WP01 → WP02 → WP03
- **Parallelization**: Within WP03, test subtasks T012/T013/T014 can run in parallel.
- **MVP Scope**: WP01 + WP02 deliver the functional contract changes. WP03 adds integration quality.

---

## Requirements Coverage Summary

| Requirement ID | Covered By Work Package(s) |
|----------------|----------------------------|
| FR-001 | WP01 |
| FR-002 | WP01 |
| FR-003 | WP01 |
| FR-004 | WP01 |
| FR-005 | WP02 |
| FR-006 | WP01 |
| FR-007 | WP02, WP03 |
| NFR-001 | WP02, WP03 |
| NFR-002 | WP03 |
| NFR-003 | WP03 |
| C-001 | WP01 |
| C-002 | WP01, WP02 |
| C-003 | WP01 |
| C-004 | WP01 |

---

## Subtask Index (Reference)

| Subtask ID | Summary | Work Package | Priority | Parallel? |
|------------|---------|--------------|----------|-----------|
| T001 | Bump CONNECTOR_SCHEMA_VERSION to 2.8.0 | WP01 | P0 | No |
| T002 | Add user_id to existing connector payloads | WP01 | P0 | No |
| T003 | Add USER_CONNECTED/USER_DISCONNECTED constants | WP01 | P0 | No |
| T004 | Create UserConnectedPayload and UserDisconnectedPayload | WP01 | P0 | No |
| T005 | Create UserConnectionStatus model | WP01 | P0 | No |
| T006 | Add user_connections to ReducedConnectorState | WP02 | P0 | No |
| T007 | Update reducer for per-user roster from user_id | WP02 | P0 | No |
| T008 | Handle UserConnected/UserDisconnected in reducer | WP02 | P0 | No |
| T009 | Add anomaly for UserDisconnected without UserConnected | WP02 | P0 | No |
| T010 | Update __init__.py exports | WP03 | P1 | No |
| T011 | Regenerate JSON schemas | WP03 | P1 | No |
| T012 | Update unit tests for payloads/constants | WP03 | P1 | Yes |
| T013 | Update reducer tests for roster | WP03 | P1 | Yes |
| T014 | Extend property tests with new events | WP03 | P1 | Yes |

<!-- status-model:start -->
## Canonical Status (Generated)
- WP01: done
- WP02: done
- WP03: done
<!-- status-model:end -->
